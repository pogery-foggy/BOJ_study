# 누적합 핵심

## 1. 한 문장 기억

변하지 않는 배열의 구간 합을 여러 번 묻는다면, 앞에서부터 합을 한 번 저장해 두고 경계의 차로 답한다.

## 2. 언제 꺼내는가

- 정적 배열에서 구간 합·구간 내 조건 발생 횟수 질의가 많다.
- 2차원 직사각형 합을 반복해서 구한다.
- 다른 알고리즘이 정한 구간의 합을 O(1)에 평가해야 한다.

## 3. 핵심 상태 / 불변식

- 1차원 `ps[i]`는 첫 i개, 즉 `[1,i]`의 합이다. 따라서 `[l,r] = ps[r]-ps[l-1]`.
- 2차원 `ps[y][x]`는 좌상단 `(1,1)`부터 `(y,x)`까지의 합이다.
- 0행·0열을 0으로 두면 경계 분기가 사라진다.

## 4. C++ 최소 구현 골격

```cpp
vector<long long> ps(n + 1, 0);
for (int i = 1; i <= n; ++i) {
    cin >> a[i];
    ps[i] = ps[i - 1] + a[i];
}
long long rangeSum(int l, int r) { // inclusive
    return ps[r] - ps[l - 1];
}

// 2차원
ps[y][x] = a[y][x] + ps[y-1][x] + ps[y][x-1] - ps[y-1][x-1];
long long rect = ps[y2][x2] - ps[y1-1][x2]
               - ps[y2][x1-1] + ps[y1-1][x1-1];
```

## 5. 빈 화면 구현 순서

1. `ps[i]`가 어디까지 포함하는지 선언한다.
2. 원본을 1번 인덱스부터 저장한다.
3. 누적 점화식을 작성한다.
4. 질의 경계를 모두 inclusive로 통일한다.
5. 빼기 두 번, 중복 제거분 더하기 한 번을 쓴다.
6. 길이 1 구간과 전체 구간을 검사한다.

## 6. 자주 틀리는 지점

- `[l,r]`에서 `ps[l]`을 빼 첫 원소를 누락하는 실수.
- 2차원에서 좌상단 중복 영역을 다시 더하지 않는 실수.
- 합을 `int`로 두어 overflow하는 실수.
- 값 갱신이 있는 문제에 정적 누적합을 쓰는 실수.

## 7. 내 코드에서 다시 보기

- [2167.cpp](./2167.cpp): 2차원 누적합과 직사각형 포함-배제의 기본형
- [21318.cpp](./21318.cpp): 원소가 아니라 “감소가 발생한 지점”을 0/1로 누적
- [1989.cpp](./1989.cpp): 모노톤 스택으로 정한 구간 합을 누적합으로 O(1) 계산

> `11660.cpp`는 현재 내용이 비어 있으므로 복습 대표 코드로 사용하지 않았다.
