#include <cmath>
#include <iostream>

using namespace std;

void init() {}

void solve() {}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    init();
    solve();
    long long N, M;
    cin >> N >> M;
    cout << (long long)abs(N - M);
    return 0; // 정상종료시 반드시 0을 리턴해야합니다.
}