# 문자열 처리 핵심

## 1. 한 문장 기억

문자열 문제는 문자를 하나씩 읽으며 지금 어느 구간에 있는지와 직전까지 확정된 결과만 유지한다.

## 2. 언제 꺼내는가

- 단어 뒤집기, 빈도 계산, 진법 변환, 팰린드롬처럼 문자 단위 규칙을 구현한다.
- 특정 패턴의 연속 등장 횟수를 선형으로 센다.
- 공백을 포함한 한 줄 전체를 상태에 따라 처리해야 한다.

## 3. 핵심 상태 / 불변식

- 인덱스 `i` 이전 문자는 모두 처리 완료다.
- 스택/버퍼에는 아직 출력하지 않은 현재 단어만 들어 있다.
- 패턴 탐색에서는 `correct`가 현재 위치까지 연속된 패턴 조각의 수를 뜻한다.

## 내 코드 스타일 C++ 최소 구현 골격

스타일 근거: [17413.cpp](./17413.cpp)의 전역 문자열, `getline` 입력, 지역 `stack<char>`, 인덱스 `for`문과 스택을 직접 비우는 출력 방식을 축약했다. 원본에 없던 람다는 쓰지 않았다.

```cpp
string s;

void init() { getline(cin, s); }

void solve() {
    stack<char> st;
    for (int i = 0; i < s.size(); i++) {
        if (s[i] == '<') {
            while (!st.empty()) {
                char cur = st.top();
                cout << cur;
                st.pop();
            }
            cout << '<';
            while (s[++i] != '>')
                cout << s[i];
            cout << '>';
        } else if (s[i] == ' ') {
            while (!st.empty()) {
                char cur = st.top();
                cout << cur;
                st.pop();
            }
            cout << ' ';
        } else
            st.push(s[i]);
    }
    while (!st.empty()) {
        char cur = st.top();
        cout << cur;
        st.pop();
    }
}
```

원본 마지막의 불필요한 `cout << " ";`는 결과에 여분 공백을 만들 수 있어 골격에서는 제외했다. 단순 빈도 문제라면 [10808.cpp](./10808.cpp)처럼 전역 `alpha[26]`와 범위 `for`문을 쓴다.

## 5. 빈 화면 구현 순서

1. 입력이 토큰인지 공백 포함 한 줄인지 정한다.
2. 문자를 읽을 때 필요한 상태를 열거한다.
3. 상태 전환 문자와 일반 문자를 분리한다.
4. 미완성 버퍼를 비우는 함수를 먼저 만든다.
5. 한 번의 선형 순회로 처리한다.
6. 문자열 끝에서 남은 버퍼를 반드시 처리한다.

## 6. 자주 틀리는 지점

- `cin >>` 뒤 바로 `getline`해 남은 개행을 읽는 실수.
- `s[i+1]`, `s[i+2]`를 경계 확인 없이 읽는 실수.
- 공백이나 태그 시작 직전에 쌓인 단어를 출력하지 않는 실수.
- 마지막 단어를 루프 밖에서 비우지 않는 실수.

## 7. 내 코드에서 다시 보기

- [17413.cpp](./17413.cpp): 태그 안/밖을 나누고 단어만 스택으로 뒤집는 상태 순회
- [5525.cpp](./5525.cpp): `IOI` 연속 길이를 재사용해 패턴을 O(N)에 세는 방식
- [10808.cpp](./10808.cpp): 문자를 인덱스로 바꿔 빈도를 세는 기본형
